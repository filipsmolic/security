{
  "buildCommand": "npm install && npm run build",
  "outputDirectory": "dist/vulnerable-web-app",
  "devCommand": "npm start",
  "framework": "angular",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}

